---
title: "SSH 端口转发"
description: "教你掌握端口转发的三种方式"
pubDate: "NOV 20 2024"
image: /image/ToolUse/SSH端口转发.png
categories:
  - 网络安全
badge: Pin
tags: 
  - 工具技巧
---
import Success from "../../../components/blog/success.astro"; 

## 本地端口转发
本地转发（local forwarding）指的是，创建一个本地端口，将发往该端口的所有通信都通过 SSH 服务器，转发到指定的远程服务器的端口。这种情况下，SSH 服务器只是一个作为跳板的中介，用于连接本地计算机无法直接连接的远程服务器。本地转发是在本地计算机建立的转发规则。

### 需求
现在有一台 SSH 服务器tunnel-host，我们想要通过这台机器，在本地2121端口与目标网站www.example.com的80端口之间建立 SSH 隧道，就可以写成下面这样。
<Success>ssh -NTL 2121:www.example.com:80 tunnel-host -N</Success>

## 远程端口转发

### 需求
我现在有一台有代理功能的主机和一台无代理功能的云端VPS，我的想法是让VPS的流量能够通过我的主机走代理。
```
本地主机：
Clash：10005

远端VPS:
IP：1.2.3.4
user: root
```
### 步骤
目的：将远端VPS的10005端口映射到本机10005端口。

1、在本地主机上执行：
<Success>ssh -NTR 10005:localhost:10005 -p 29998 root@1.2.3.4</Success>

2、在VPS上执行：
<Success>proxychains4 curl http://www.google.com</Success>

## <font color = "#749DD6">Continued</font>